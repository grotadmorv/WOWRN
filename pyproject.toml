[tool.black]
line-length = 88
target-version = ['py310']

[tool.isort]
profile = "black"
line_length = 88
src_paths = ["src"]

[tool.pytest.ini_options]
pythonpath = [
    ".", "src"
]
testpaths = ["tests"]
